---
import BaseLayout from "../../layouts/BaseLayout.astro";
import MemoryMatch from "../../games/MemoryMatch.tsx";
---

<BaseLayout title="Memory Match | Muhammad Hisyam">
    <div class="min-h-screen bg-navy py-20 px-6">
        <div class="max-w-2xl mx-auto">
            <!-- Back Button -->
            <a
                href="/#mini-games"
                class="inline-flex items-center gap-2 text-mint hover:text-mint/80 transition-colors mb-8 font-mono text-sm"
            >
                <svg
                    class="w-4 h-4"
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                >
                    <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M15 19l-7-7 7-7"></path>
                </svg>
                Back to Mini Games
            </a>

            <!-- Game Container -->
            <div
                class="bg-light-navy rounded-xl p-6 border border-slate/10 shadow-xl"
            >
                <h1
                    class="text-3xl font-serif font-bold text-light-slate mb-2 text-center"
                >
                    🃏 Memory Match
                </h1>
                <p class="text-slate text-center mb-6 text-sm">
                    Flip cards and find matching pairs — test your memory!
                </p>

                <MemoryMatch client:load />
            </div>
        </div>
    </div>
</BaseLayout>
